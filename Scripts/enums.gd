extends Node

enum Screen {
	LAST,
	SELECTIONS,
	SELECTION_EDITOR,
	DECISIONS,
	DECISION_EDITOR,
	OPTIONS,
	OPTION_EDITOR,
	OPTIONS_SELECTOR,
	CONSTRAINTS,
	CONSTRAINTS_SELECTOR,
	PREFERENCE_CONSTRAINT_EDITOR,
	COMPOSITE_CONSTRAINT_EDITOR,
	SETTINGS
}

enum ConstraintType {
	PREF = 0,
	COMP = 1
}
